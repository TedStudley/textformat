require 'spec_helper'

describe Textformat do
  it 'has a version number' do
    expect(Textformat::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
